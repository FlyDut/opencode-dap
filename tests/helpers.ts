import type { DapResolvedAdapter } from "../src/types";

export const TEST_ADAPTER: DapResolvedAdapter = {
  name: "test-adapter",
  command: "test-adapter",
  args: [],
  resolvedCommand: "test-adapter",
  languages: [],
  fileTypes: [],
  rootMarkers: [],
  launchDefaults: {},
  attachDefaults: {},
  connectMode: "stdio",
  acceptsDirectoryProgram: false,
};
